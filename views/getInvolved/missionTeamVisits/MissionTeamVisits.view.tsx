import styles from './MissionTeamVisits.module.scss';

type Props = {};

const MissionTeamVisits = (props: Props) => {
  return <div className={styles.container}>MissionTeamVisits.view</div>;
};

export default MissionTeamVisits;
