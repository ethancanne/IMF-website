import styles from './Ministries.module.scss';

type Props = {};

const Ministries = (props: Props) => {
  return <div className={styles.container}>Ministries.view</div>;
};

export default Ministries;
